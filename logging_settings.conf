# Logging configuration
# Valid levels: debug, info, warning, off
#
# terminal  = Console output during development
# sessions  = File-based session logs in logs/app/
# conversations = Per-conversation logs in logs/conversations/
# retention_hours = Auto-delete logs older than this many hours (0 = disabled)

terminal = info
sessions = info
conversations = info
retention_hours = 48
