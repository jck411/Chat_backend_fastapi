"""Tests for environment handling within the chat orchestrator.

The backend no longer builds environment variables for MCP servers since
it connects to already-running external servers by URL only.
"""
